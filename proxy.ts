// NextAuth v5 doesn't require a proxy file for basic auth
// The auth is handled through the route handlers and callbacks
export function proxy() {
  // Placeholder for Next.js proxy requirement
  return undefined;
}

export const config = {
  matcher: [],
};